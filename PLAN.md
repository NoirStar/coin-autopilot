# Coin Autopilot — BTC 흐름 기반 자동매매 플랫폼

## 1. 프로젝트 개요

### 1.1 비전

BTC 흐름을 선행 신호로 활용하여 알트코인에서 수익을 추구하는 자동매매 플랫폼.
업비트(현물)와 OKX(선물)를 운용하며, 백테스팅 → 가상매매 → 실전매매를 단계적으로 전환한다.

**핵심 차별화**: 투명한 웹 기반 시그널 페이지. 텔레그램 시그널방과 달리 각 시그널의 근거(z-score, RSI, BTC 레짐 상태)를 공개. 공개 페이지(인증 불필요) + 로그인 시 개인 백테스팅/가상매매 대시보드.

### 1.2 핵심 목표

| 목표 | 설명 |
|------|------|
| **안전 우선** | MDD 제한, 레짐 필터, 포지션 사이징으로 "잃지 않는" 방향 |
| **전략 검증** | 백테스팅 → 가상매매 → 실전 3단계 파이프라인 |
| **투명한 시그널** | 공개 시그널 페이지에 매매 근거 투명하게 공개 |
| **실시간 모니터링** | 웹 대시보드에서 자산/손익/전략 상태 확인 |
| **다중 전략 비교** | 여러 전략을 동시 가상매매하여 최적 전략 선별 |
| **확장성** | 전략 플러그인 구조, 타임프레임 설정 가능 |

### 1.3 거래소

| 거래소 | 용도 | 마켓 | 비고 |
|--------|------|------|------|
| **업비트** | 현물 매매 | KRW 마켓 | JWT 인증, 초당 8회 제한 |
| **OKX** | 선물(무기한) 매매 | USDT 페어 | HMAC-SHA256, 초당 20회 |

---

## 2. 아키텍처

### 2.1 시스템 구성도

```
┌──────────────────────────────────────────────────────────────┐
│                    Web Dashboard (Vercel)                      │
│          React 19 · Vite · shadcn/ui · TailwindCSS            │
│    ┌───────────┬──────────┬──────────┬──────────┬──────────┐  │
│    │시그널(공개)│대시보드   │백테스팅   │가상매매   │설정      │  │
│    └───────────┴──────────┴──────────┴──────────┴──────────┘  │
│                     ▲ Supabase Client SDK                      │
└─────────────────────┼──────────────────────────────────────────┘
                      │
┌─────────────────────┼──────────────────────────────────────────┐
│                  Supabase (PostgreSQL)                          │
│    candles · signals · regime_states · backtest_results         │
│    positions · auth.users                                      │
│    RLS: 공개 테이블(anon read) + 개인 테이블(auth+user_id)     │
└─────────────────────┼──────────────────────────────────────────┘
                      │
┌─────────────────────┼──────────────────────────────────────────┐
│               Trading Server (VPS, 24시간)                      │
│            Node.js · Hono · node-cron · Supabase                │
│    ┌──────────┬──────────┬──────────┬──────────┬──────────┐    │
│    │캔들 수집  │지표 계산  │레짐 판단  │시그널 생성│REST API  │    │
│    │(node-cron)│(EMA,RSI) │(Risk-On  │(전략     │(Hono)    │    │
│    │          │(ATR,z)   │ /Off)    │ 평가)    │          │    │
│    ├──────────┼──────────┼──────────┼──────────┼──────────┤    │
│    │백테스트   │가상매매   │Supabase  │          │          │    │
│    │엔진      │매니저    │서비스    │          │          │    │
│    └──────────┴──────────┴──────────┴──────────┴──────────┘    │
│         ▲ REST API                                              │
└─────────┼──────────────────────────────────────────────────────┘
          │
    ┌─────┴─────┐              ┌───────────┐
    │  Upbit    │              │   OKX     │
    │  (현물)   │              │  (선물)   │
    └───────────┘              └───────────┘
```

### 2.2 데이터 흐름

```
VPS (24시간)                     Supabase                     Vercel
┌──────────────┐                ┌─────────────────┐          ┌──────────┐
│ node-cron    │──WRITE──────▶  │ candles         │ ◀─READ── │ 공개     │
│ (4H 주기)    │                │ signals         │          │ 시그널   │
│              │                │ regime_states   │          │ 페이지   │
│ 지표 계산    │──시그널────▶   │ backtest_results│          │          │
│ 레짐 판단    │                │ positions       │ ◀─READ── │ 개인     │
│ 시그널 생성   │               │ users (Auth)    │          │ 대시보드 │
└──────┬───────┘                └─────────────────┘          └──────────┘
       │ REST API                      ▲
  ┌────┴────┐                          │ RLS
  │ Upbit   │ ┌────────┐        공개: anon read
  │ OKX     │ │        │        개인: auth + user_id
  └─────────┘ └────────┘
```

- VPS 크론(node-cron)이 4시간마다 데이터 수집 → 시그널 생성 → Supabase 저장
- Vercel 프론트엔드는 Supabase Client SDK로 직접 읽기
- 공개 페이지는 React Query로 폴링 (4시간 refetch), 실시간 구독 불필요
- VPS → Supabase는 `SUPABASE_SERVICE_ROLE_KEY`로 쓰기 (RLS 우회)

### 2.3 기술 스택

| 레이어 | 기술 | 선택 이유 |
|--------|------|-----------|
| **서버** | Node.js, TypeScript | 프론트엔드와 같은 언어, WebSocket 지원 |
| | Hono | 경량 고성능 HTTP 프레임워크 |
| | node-cron | 4시간 주기 스케줄링 |
| | technicalindicators | EMA, RSI, ATR, 볼린저밴드 |
| | decimal.js | 금융 연산 정밀도 |
| | Supabase JS | PostgreSQL + Auth + Realtime |
| | zod | 입력 검증 |
| **웹** | React 19, Vite, TypeScript | SPA, 빠른 빌드 |
| | shadcn/ui, Radix UI | 접근성 기반 컴포넌트 |
| | Tailwind CSS 4 | 유틸리티 스타일링 |
| | Lucide React | 아이콘 (이모지 대신) |
| | Lightweight Charts | 캔들스틱 차트 |
| | Recharts | 성과/통계 차트 |
| | Zustand | 클라이언트 상태 관리 |
| | TanStack Query | 서버 상태 (Supabase 페칭) |
| **배포** | Vercel | 프론트엔드 자동 배포 |
| | VPS (로컬/클라우드) | 서버 24시간 실행 (pm2) |

### 2.4 디자인 시스템

**Terminal Ink** — 데이터 중심 미니멀 다크 UI. 상세 명세는 `DESIGN.md` 참조.

| 요소 | 값 |
|------|-----|
| **Background** | `#0a0a0c` — 순수 다크 |
| **Surface** | `#111114` — flat 카드 (글래스 효과 금지) |
| **Profit** | `#2dd4a8` — teal cyan |
| **Loss** | `#f87171` — soft rose |
| **Accent** | `#a78bfa` — muted violet (제한적 사용) |
| **Warning** | `#fbbf24` — amber |
| **Info** | `#60a5fa` — blue |
| **본문 폰트** | Geist |
| **숫자 폰트** | JetBrains Mono (tabular-nums) |
| **아이콘** | Lucide React (이모지 사용 금지) |

**UX 규칙:**
- 전문 용어(레짐, z-score, Sharpe 등)에 `?` 아이콘 + 툴팁으로 쉬운 설명 표시
- 이모지 금지, Lucide 아이콘만 사용
- 다크 모드 전용 (트레이딩 UI 표준)
- opacity 트릭 금지 — 실제 색상 토큰만 사용
- 장식적 요소(글래스, 그라데이션 배경, 노이즈) 금지

---

## 3. 매매 전략

### 3.1 전략 플러그인 구조

모든 전략은 `Strategy` 인터페이스를 구현. 타임프레임은 전략별 설정 가능 (하드코딩 금지).

```typescript
interface Strategy {
  config: {
    id: string
    name: string
    timeframe: '1h' | '4h' | '1d'  // 설정 가능
    exchange: 'upbit' | 'okx'
    params: Record<string, number>
  }
  evaluate(candles, regime): StrategySignal[]
  evaluateExits(candles, regime, positions): ExitSignal[]
}
```

나중에 모멘텀 전략, 볼륨 스파이크 전략, 알트 독립 펌핑 감지 전략 등을 같은 인터페이스로 추가 가능.

### 3.2 전략 목록

#### 전략 1: BTC 레짐 필터 + 알트 평균회귀 (구현 완료)

> BTC가 리스크-온을 확인해준 뒤, 알트가 BTC 대비 눌린 상태를 매수

**BTC 레짐 판단 (4H 기준)**
```
Risk-On 조건 (모두 충족):
  ① BTC Close > EMA(200, 4H)
  ② BTC RSI(14, 4H) ∈ [52, 70]
  ③ BTC ATR%(14, 4H) ≤ 4.5%

Risk-Off 조건 (하나라도 충족):
  ① BTC Close < EMA(200, 4H)
  ② BTC ATR%(14, 4H) ≥ 6.5%
  ③ BTC RSI(14, 4H) ≤ 45

경계값: EMA와 가격이 같으면 이전 레짐 유지
```

**알트 진입/청산**
```
비율:    R_i = ln(ALT_i / BTC)
z-score: z_i = (R_i − SMA_20(R_i)) / STD_20(R_i)

진입 (Risk-On + 모두 충족):
  ① z_i ≤ −1.0
  ② ALT RSI(14, 4H) ≤ 78
  ③ 동시 보유 ≤ 5종목

청산 (우선순위):
  0. 손절: 진입가 − 2.7 × ATR(14, 4H) 이탈 → 즉시
     레짐 스톱: BTC Risk-Off → 전체 포지션 청산
  1. 이익 실현: z_i ≥ 0.0
  2. 시간 청산: 8캔들(32시간) 경과
```

#### 전략 2: BTC 도미넌스 로테이션 (미구현)

> BTC.D 하락 + USDT.D 하락 = 알트 시즌 포착

#### 전략 3: BTC 변동성 타이밍 (미구현)

> 변동성 낮고 추세 양호 → 알트 익스포저 확대

#### 전략 4: 펀딩비 차익 (미구현, OKX 선물 전용)

> 펀딩비 극단 시 델타중립 포지션

#### 전략 5: 김프 모니터링 (미구현)

> 업비트-OKX 간 가격 괴리 추적

### 3.3 투자 성향 프로필

| 프로필 | 레버리지 | 동시 보유 | MDD 한도 | ATR 손절 | 전략 |
|--------|----------|-----------|----------|----------|------|
| **안전** | 1x (현물만) | 3종목 | 10% | 2.0×ATR | 전략1 단독 |
| **중립** | 1~2x | 5종목 | 15% | 2.7×ATR | 전략1 + 전략3 |
| **공격** | 2~3x | 8종목 | 25% | 3.5×ATR | 전략1~4 조합 |

### 3.4 포지션 사이징 & 리스크 관리

```
일일 최대 손실   = Equity × 2%
단일 포지션 손실 = Equity × 0.30%
수량 계산        = (Equity × 0.003) / (STOP_ATR_MULT × ATR)
동시 보유 상한   = 프로필별 3~8종목

MDD 15% 초과 → 리스크 50% 축소
MDD 25% 초과 → 매매 중단 + 알림

선물 추가 규칙:
  - 마진 모드: 격리(Isolated)
  - 지갑 증거금 사용률 < 35%
  - 모든 청산 주문: reduce-only
  - 레버리지 상한: 프로필별 1~3x
```

### 3.5 알트코인 유니버스

현재 대상 (업비트 KRW 마켓):
**BTC, ETH, XRP, SOL, DOGE, ADA, AVAX, DOT, LINK, ATOM**

선정 기준:
- 30일 평균 일거래대금 ≥ 50억 KRW
- 스프레드 ≤ 20bp
- 스테이블코인/랩드/레버리지 토큰 제외
- 신규 상장 14일 이내 제외

---

## 4. 백테스팅 시스템

### 4.1 간이 백테스트 (구현 완료)

| 항목 | 설정 |
|------|------|
| **데이터** | 최근 6개월 4H 캔들 (Upbit REST API) |
| **수수료** | 업비트 0.05% (매수/매도 각각) |
| **슬리피지** | 다음 4H 캔들 시가 기준 ±0.1% |
| **체결** | 시그널 발생 → 다음 캔들 시가에 체결 |
| **워크포워드** | 미적용 (본격 백테스트에서 추가) |
| **생존편향** | 미제거 (본격 백테스트에서 추가) |

성과 지표: Sharpe Ratio, MDD, 승률, 총 거래수, CAGR, 평균 보유시간

### 4.2 본격 백테스트 (미구현, TODOS 참조)

- 생존편향 제거: 과거 시점별 거래 가능 유니버스 재구성
- 워크포워드: 70% 학습 / 30% 검증, 롤링 윈도우
- 스트레스 테스트: 차이나 크래시, LUNA, FTX, 엔캐리 등

---

## 5. 가상매매 (미구현)

- 최대 10개 전략 동시 실행
- 현재 호가 기반 가상 체결 (슬리피지 모델)
- 전략 간 성과 비교 + 랭킹 테이블
- 가상매매 → 실전매매 원클릭 전환

---

## 6. 프로젝트 구조

```
coin-autopilot/
├── PLAN.md
├── TODOS.md
├── CLAUDE.md
├── README.md
├── .gitignore
│
├── server/                          # Trading Server (VPS)
│   ├── package.json
│   ├── tsconfig.json
│   ├── .env                         # 실제 키 (gitignore)
│   ├── .env.example                 # 빈 템플릿 (커밋)
│   ├── src/
│   │   ├── index.ts                 # 서버 엔트리 (Hono + 크론)
│   │   ├── core/
│   │   │   └── cron.ts              # node-cron 스케줄러 (4H)
│   │   ├── data/
│   │   │   └── candle-collector.ts  # Upbit/OKX 캔들 수집
│   │   ├── db/
│   │   │   └── schema.sql           # Supabase 스키마 + RLS
│   │   ├── indicator/
│   │   │   └── indicator-engine.ts  # EMA, RSI, ATR, z-score
│   │   ├── strategy/
│   │   │   ├── strategy-base.ts     # Strategy 인터페이스
│   │   │   ├── btc-regime-filter.ts # BTC 레짐 판단
│   │   │   └── alt-mean-reversion.ts# 알트 평균회귀 전략
│   │   ├── services/
│   │   │   ├── database.ts          # Supabase 클라이언트
│   │   │   ├── backtest-engine.ts   # 간이 백테스트 엔진
│   │   │   └── signal-generator.ts  # 시그널 생성 파이프라인
│   │   ├── routes/
│   │   │   ├── signals.ts           # 시그널 API (공개)
│   │   │   ├── dashboard.ts
│   │   │   ├── strategy.ts
│   │   │   ├── backtest.ts
│   │   │   ├── paper-trading.ts
│   │   │   ├── portfolio.ts
│   │   │   └── settings.ts
│   │   └── scripts/
│   │       ├── backfill.ts          # 히스토리 캔들 일괄 수집
│   │       ├── generate-signals.ts  # 수동 시그널 생성
│   │       └── test-pipeline.ts     # 파이프라인 테스트
│   └── tests/
│
├── web/                             # React Dashboard (Vercel)
│   ├── package.json
│   ├── .env                         # Supabase anon key (gitignore)
│   ├── .env.example
│   ├── src/
│   │   ├── App.tsx
│   │   ├── main.tsx
│   │   ├── index.css                # 디자인 시스템 (Terminal Ink → DESIGN.md 참조)
│   │   ├── lib/
│   │   │   ├── supabase.ts          # Supabase 클라이언트
│   │   │   ├── transforms.ts        # snake_case → camelCase
│   │   │   ├── constants.ts
│   │   │   └── utils.ts
│   │   ├── components/
│   │   │   ├── ui/
│   │   │   │   └── term-tooltip.tsx  # 용어 설명 툴팁
│   │   │   ├── auth/
│   │   │   │   ├── AuthGuard.tsx     # 비공개 라우트 인증 가드
│   │   │   │   └── LoginModal.tsx    # 로그인 모달 (GitHub + 이메일)
│   │   │   ├── charts/
│   │   │   │   └── BtcCandleChart.tsx# TradingView 차트 위젯
│   │   │   └── layout/
│   │   │       ├── AppLayout.tsx
│   │   │       ├── Header.tsx        # 인증 통합 (로그인/로그아웃)
│   │   │       ├── Sidebar.tsx
│   │   │       └── StatusBar.tsx
│   │   ├── pages/
│   │   │   ├── SignalsPage.tsx       # 공개 시그널 페이지 (신규)
│   │   │   ├── DashboardPage.tsx
│   │   │   ├── StrategyPage.tsx
│   │   │   ├── BacktestPage.tsx
│   │   │   ├── PaperTradingPage.tsx
│   │   │   ├── PortfolioPage.tsx
│   │   │   └── SettingsPage.tsx
│   │   ├── hooks/
│   │   │   └── useAuth.ts           # Supabase Auth 훅
│   │   ├── services/
│   │   │   └── api.ts
│   │   ├── stores/
│   │   │   ├── dashboard-store.ts
│   │   │   ├── strategy-store.ts
│   │   │   ├── backtest-store.ts
│   │   │   ├── paper-trading-store.ts
│   │   │   └── settings-store.ts
│   │   └── types/
│   │       └── trading.ts
│   └── tests/
│
└── TODOS.md
```

---

## 7. UI 설계

> 모든 페이지는 DESIGN.md의 Terminal Ink 디자인 시스템을 따른다.
> 레퍼런스 스크린 첨부 이미지(Cryptowatch, CryptoTrading-Hub)의 정보 밀도를 참고하되, 장식적 요소는 배제한다.

### 7.0 글로벌 UI 규칙

#### 숫자 포매팅

| 데이터 | 포맷 | 예시 |
|--------|------|------|
| KRW 가격 | `#,###원` (1만 이상 `#.##만`, 1억 이상 `#.##억`) | `1,380원`, `5.23만`, `1.01억` |
| USD 가격 | `$#,###.##` | `$97,432.10` |
| 퍼센트 | `±#.##%` (부호 항상 표시) | `+3.42%`, `-1.23%` |
| z-score | `±#.##` (소수점 2자리) | `-1.23`, `+0.45` |
| RSI | `##.#` (소수점 1자리) | `58.3` |
| Sharpe Ratio | `#.##` (소수점 2자리) | `1.42` |
| 승률 | `##.#%` | `58.3%` |
| 거래 수량(코인) | 유효숫자 4자리 | `0.04312 BTC` |
| 타임스탬프 | 상대시간 (`2h 전`) + 호버 시 절대시간 (`2026-03-28 14:00 KST`) | `4h 전` |
| 수량/금액 | JetBrains Mono, `tabular-nums`, 우측 정렬 | — |

**색상 규칙:** 양수 → `--profit`, 음수 → `--loss`, 0 → `--text-secondary`

#### 반응형 브레이크포인트

| 뷰포트 | 너비 | 레이아웃 | 사이드바 |
|---------|------|----------|----------|
| **Mobile** | < 640px | 1열, 카드 스택 | 숨김 (햄버거 메뉴) |
| **Tablet** | 640–1023px | 2열 그리드 | 숨김 (토글 가능) |
| **Desktop** | ≥ 1024px | 4열 그리드 | 240px 고정 표시 |
| **Wide** | ≥ 1440px | 4열, max-width 1200px 중앙 | 240px 고정 표시 |

**모바일 우선 규칙:**
- 테이블 → 카드 리스트 전환 (< 640px)
- 차트 높이: 모바일 200px, 태블릿 280px, 데스크톱 360px
- 터치 타겟: 최소 44×44px
- 스와이프: 모바일 탭 전환 가능 (테이블 내 가로스크롤 금지)

#### 키보드 & 접근성

| 영역 | 키보드 | 스크린 리더 |
|------|--------|------------|
| 사이드바 내비게이션 | `Tab`/`Shift+Tab` 순회, `Enter` 선택 | `nav` 랜드마크, `aria-current="page"` |
| 데이터 테이블 | `↑↓` 행 이동, `Enter` 확장/축소 | `role="grid"`, `aria-expanded` |
| 아코디언 (시그널 근거) | `Enter`/`Space` 토글 | `aria-expanded`, `aria-controls` |
| 모달/다이얼로그 | `Escape` 닫기, 포커스 트랩 | `role="dialog"`, `aria-modal` |
| 폼 인풋 | `Tab` 순회, 에러 시 포커스 이동 | `aria-invalid`, `aria-describedby` |
| 차트 | — (시각 전용) | `aria-label`로 요약 텍스트, 상세 데이터는 테이블로 제공 |

**WCAG 2.1 AA 기준:**
- 색상 대비: 일반 텍스트 4.5:1, 큰 텍스트 3:1 (모든 토큰 충족 확인 완료)
- `--text-faint`(#52525b) on `--background`(#0a0a0c) = 4.6:1 ✓ (최저 대비)
- 시맨틱 색상은 색상만으로 정보 전달 금지 — 아이콘/텍스트 라벨 병행 필수

#### 인증 경계 UI

| 상태 | 표시 영역 | 처리 |
|------|-----------|------|
| **비로그인** | 시그널 페이지(전체 접근) + 헤더에 "로그인" 버튼 | 대시보드 클릭 시 → 로그인 모달 |
| **로그인** | 전체 접근 + 헤더에 프로필 아바타 | 사이드바 전 메뉴 활성 |
| **로그인 모달** | 중앙 모달, backdrop dim | Magic Link (이메일) 또는 GitHub OAuth |
| **세션 만료** | 토스트 알림 "세션이 만료되었습니다" + 자동 로그인 모달 | 작업 중 데이터 로컬 보존 |

**비로그인 사용자가 개인 기능 접근 시:**
모달이 아닌 인라인 메시지로 안내. "로그인하면 백테스트와 가상매매를 이용할 수 있습니다." + `LogIn` 아이콘 + CTA 버튼.

#### 공통 상태 패턴

**로딩 (스켈레톤):**
- 카드: `var(--surface)` 배경 + shimmer 애니메이션 (200ms)
- 테이블: 3행 스켈레톤, 열 너비 비율 유지
- 숫자: `----` placeholder (JetBrains Mono)
- 차트: 빈 프레임 + 중앙 `Loader2` 스피너

**에러:**
- 인라인: `AlertTriangle` 아이콘 + 메시지 + "다시 시도" 버튼
- 전체: 카드 내부에 에러 상태, 다른 섹션은 정상 표시 (전체 페이지 에러 금지)
- 색상: `--loss` 아이콘, `--text-secondary` 메시지

**빈 상태:**
- 중앙 정렬, `1px dashed var(--border)` 테두리
- Lucide 아이콘(size 8) + 주요 메시지(13px, `--text-secondary`) + 보조 메시지(11px, `--text-muted`)
- 가능하면 CTA 버튼 포함 ("첫 백테스트 실행하기" 등)

---

### 7.1 공개 시그널 페이지

> 핵심 목표: "투명한 매매 근거"가 첫 화면에서 즉시 보여야 함. 아코디언에 숨기지 않는다.

#### 정보 계층구조

```
┌─────────────────────────────────────────────────────────────┐
│  HEADER: Coin Autopilot + [시그널·] [대시보드] [로그인]      │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ① BTC 레짐 상태 바 (compact, 전체 너비)                    │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ ◉ RISK-ON  BTC 1.01억  EMA↑  RSI 58.3  ATR 3.2%     │  │
│  │                                       업데이트: 2h 전  │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                             │
│  ② 시그널 카드 (핵심 — 근거가 즉시 보임)                    │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ ETH  매수  z: -1.23  RSI: 42.1             4h 전      │  │
│  │ ─────────────────────────────────────────────────────  │  │
│  │ ✓ BTC 레짐 Risk-On  ✓ z ≤ -1.0  ✓ RSI ≤ 78          │  │
│  │ 손절: 진입가 -2.7×ATR (₩2,847,000)                    │  │
│  └───────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ SOL  매수  z: -1.45  RSI: 38.7             4h 전      │  │
│  │ ─────────────────────────────────────────────────────  │  │
│  │ ✓ BTC 레짐 Risk-On  ✓ z ≤ -1.0  ✓ RSI ≤ 78          │  │
│  │ 손절: 진입가 -2.7×ATR ($142.30)                       │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                             │
│  ③ 백테스트 성과 요약 (신뢰도 근거)                         │
│  Sharpe 1.42 · 승률 58.3% · MDD -12.3% · 125건            │
│                                                             │
│  ④ CTA 배너                                                │
│  "직접 백테스트 해보기 →" [로그인 필요]                      │
│                                                             │
│  ⑤ 김치 프리미엄 (폴드 아래, 보조 정보)                     │
│  김프: +1.8% | KRW/USD: 1,380원                            │
│                                                             │
│  FOOTER: "투자 조언이 아닙니다. 투자 판단은 본인 책임"      │
└─────────────────────────────────────────────────────────────┘
```

**시그널 페이지 설계 원칙:**
- 근거(체크리스트)가 시그널과 같은 카드에 항상 보임 (아코디언 제거)
- BTC 레짐은 compact 바로 축소 — 시그널이 주인공
- 김프 모니터는 폴드 아래로 이동 (전략과 직접 연관 없음)
- CTA로 개인 기능(백테스트, 가상매매)으로 전환 유도

#### 인터랙션 상태

| 섹션 | 로딩 | 빈 상태 | 에러 | 성공 |
|------|------|---------|------|------|
| BTC 레짐 바 | 텍스트 shimmer | "데이터 수집 대기 중" + `Clock` | "BTC 데이터 연결 실패" + 재시도 | 레짐 + 지표 인라인 |
| 시그널 카드 | 2개 카드 스켈레톤 | **Risk-Off:** `ShieldCheck` + "시장 보호 모드 — 매수 억제 중" | "시그널 불러오기 실패" + 재시도 | 시그널 카드 목록 |
| | | **Risk-On + 0건:** `Search` + "조건 충족 종목 없음. 다음 4H 업데이트를 기다리세요." | | |
| 백테스트 성과 | 숫자 `----` | "백테스트 미실행" | — | 지표 4개 인라인 |
| 김프 모니터 | "계산 중..." | "환율 데이터 없음" | "API 연결 실패" | 김프 % + 환율 |

#### 반응형

| 뷰포트 | 레짐 바 | 시그널 | 성과 요약 |
|---------|---------|--------|-----------|
| Desktop (≥1024) | 1행 가로 배치 (레짐 + 지표 4개) | 카드 리스트, max-width 896px | 4개 인라인 |
| Tablet (640–1023) | 1행, 지표 줄바꿈 가능 | 카드 리스트 | 2×2 그리드 |
| Mobile (<640) | 레짐 배지만, 탭하면 지표 확장 | 카드 풀너비 | 2×2 그리드 |

---

### 7.2 대시보드 페이지

> 핵심 목표: 로그인 후 첫 화면. "내 자산 + 전략 상태 + 포지션"을 한눈에 파악.

#### 정보 계층구조

```
┌──────────────────────────────────────────────────────────────┐
│  ① KPI 바 (4열, 가장 큰 시각적 무게)                         │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐    │
│  │총 자산    │ │일간 PnL  │ │승률      │ │MDD           │    │
│  │₩12.5M    │ │+₩234K   │ │58.3%     │ │-8.2%         │    │
│  │+2.1% ↑   │ │+1.9% ↑  │ │62/106    │ │한도 15%      │    │
│  └──────────┘ └──────────┘ └──────────┘ └──────────────┘    │
│                                                              │
│  ② 메인 영역 (2열: 좌=차트, 우=사이드 패널)                  │
│  ┌─────────────────────────┐ ┌──────────────────────────┐    │
│  │ 자산 추이 차트            │ │ BTC 레짐 상태            │    │
│  │ (Recharts AreaChart)     │ │ ◉ RISK-ON  2h 전         │    │
│  │ [1W] [1M] [3M] [ALL]    │ │ EMA↑ RSI 58.3 ATR 3.2%  │    │
│  │                          │ │ ─────────────────────── │    │
│  │  ╱──╲    ╱──╲           │ │ 활성 전략: 1/4           │    │
│  │ ╱    ╲──╱    ──         │ │ • 알트 평균회귀 [ON]     │    │
│  │                          │ │ • 도미넌스 로테이션 [-]  │    │
│  └─────────────────────────┘ │ • 변동성 타이밍 [-]      │    │
│                               │ ─────────────────────── │    │
│  ③ 활성 포지션 테이블         │ MDD 게이지               │    │
│  ┌─────────────────────────┐ │ ████████░░ 8.2%/15%      │    │
│  │종목│방향│진입가│현재PnL │ └──────────────────────────┘    │
│  │ETH │매수│₩3.2M│+2.3%  │                                  │
│  │SOL │매수│₩152K│-0.8%  │                                  │
│  │... │    │     │       │                                  │
│  └─────────────────────────┘                                 │
│                                                              │
│  ④ 최근 시그널 피드 (하단, 보조)                              │
│  [4h 전] ETH 매수 z:-1.23 · [8h 전] SOL 청산 시간만료        │
└──────────────────────────────────────────────────────────────┘
```

**대시보드 설계 원칙:**
- KPI 바가 첫 뷰포트의 앵커 — 자산 상태를 즉시 전달
- 좌측 = 시각적 데이터(차트, 테이블), 우측 = 상태 패널(레짐, 전략, MDD)
- 포지션 테이블은 활성 포지션만 표시 (히스토리는 포트폴리오 페이지)
- 시그널 피드는 시간순, 최근 10건

#### 인터랙션 상태

| 섹션 | 로딩 | 빈 상태 | 에러 |
|------|------|---------|------|
| KPI 바 | 4개 카드 shimmer + `----` | 최초 로그인: "아직 거래 기록이 없습니다" | 개별 KPI 에러 표시 |
| 자산 차트 | 빈 프레임 + `Loader2` | `LineChart` 아이콘 + "가상매매를 시작하면 자산 추이가 표시됩니다" + CTA "첫 가상매매 시작" | "차트 데이터 로드 실패" + 재시도 |
| 포지션 테이블 | 3행 스켈레톤 | `Inbox` 아이콘 + "활성 포지션 없음" | "포지션 데이터 실패" + 재시도 |
| 레짐 상태 | shimmer 바 | — (항상 데이터 있음, 공개) | "BTC 데이터 실패" |
| MDD 게이지 | shimmer 바 | 게이지 0% 표시 | — |

#### 반응형

| 뷰포트 | KPI | 메인 영역 | 포지션 |
|---------|-----|-----------|--------|
| Desktop | 4열 인라인 | 2열 (차트 60% + 사이드 40%) | 테이블 |
| Tablet | 2×2 그리드 | 1열 (차트 → 사이드 패널 순) | 테이블 |
| Mobile | 2×2, 축약 라벨 | 1열 스택 | 카드 리스트 |

---

### 7.3 전략 페이지

> 핵심 목표: 사용 가능한 전략 목록 + 투자 성향 선택 + 전략 파라미터 조정.

#### 정보 계층구조

```
┌──────────────────────────────────────────────────────────────┐
│  ① 투자 성향 선택 (라디오 카드, 3열)                          │
│  ┌───────────┐ ┌────────────┐ ┌───────────┐                  │
│  │ 🛡 안전    │ │ ⚖ 중립     │ │ ⚡ 공격    │                  │
│  │ 1x 레버리지│ │ 1~2x       │ │ 2~3x      │                  │
│  │ MDD 10%   │ │ MDD 15%    │ │ MDD 25%   │                  │
│  │ [Shield]   │ │ [Scale]    │ │ [Zap]     │                  │
│  └───────────┘ └────[선택됨]─┘ └───────────┘                  │
│                                                              │
│  ② 전략 목록 (카드 그리드)                                    │
│  ┌──────────────────────────────┐ ┌─────────────────────────┐│
│  │ 알트 평균회귀  [ON] ●        │ │ 도미넌스 로테이션 [OFF] ││
│  │ BTC Risk-On 시 z-score 눌림 │ │ BTC.D↓ + USDT.D↓ 포착  ││
│  │ 매수                         │ │ [미구현]                 ││
│  │ ────────────────────────── │ │                          ││
│  │ 타임프레임: [4H ▼]          │ │                          ││
│  │ z-score 임계: [-1.0]        │ │                          ││
│  │ RSI 상한: [78]              │ │                          ││
│  │ ATR 손절 배수: [2.7]        │ │                          ││
│  │ ────────────────────────── │ └─────────────────────────┘│
│  │ [저장] [기본값 복원]         │                            │
│  └──────────────────────────────┘                            │
│  ┌──────────────────────────────┐ ┌─────────────────────────┐│
│  │ 변동성 타이밍 [OFF]          │ │ 펀딩비 차익 [OFF]       ││
│  │ [미구현]                     │ │ [미구현]                 ││
│  └──────────────────────────────┘ └─────────────────────────┘│
└──────────────────────────────────────────────────────────────┘
```

**전략 페이지 설계 원칙:**
- 성향 선택이 상단 — 이것이 리스크 파라미터를 결정
- 활성(구현 완료) 전략은 파라미터 편집 가능, 미구현 전략은 disabled + "개발 중" 배지
- 파라미터 변경 시 "저장" 버튼 활성화, 변경 전/후 비교 표시
- 전략 ON/OFF 토글로 가상매매/실전매매에 포함/제외

#### 인터랙션 상태

| 섹션 | 로딩 | 빈 상태 | 에러 |
|------|------|---------|------|
| 성향 카드 | shimmer 3열 | — (항상 3개 표시) | — |
| 전략 카드 | shimmer 4열 | — (항상 표시, 미구현은 disabled) | "설정 로드 실패" + 재시도 |
| 파라미터 저장 | 버튼 로딩 스피너 | — | 토스트: "저장 실패" + 재시도 |

#### 반응형

| 뷰포트 | 성향 카드 | 전략 카드 |
|---------|-----------|-----------|
| Desktop | 3열 인라인 | 2열 그리드 |
| Tablet | 3열 인라인 | 1열 스택 |
| Mobile | 가로 스크롤 snap | 1열 스택 |

---

### 7.4 백테스트 페이지

> 핵심 목표: 전략 + 기간 → 실행 → 결과(에쿼티 커브, 성과 지표, 거래 목록) 확인.

#### 정보 계층구조

```
┌──────────────────────────────────────────────────────────────┐
│  ① 백테스트 설정 패널 (좌측 또는 상단)                        │
│  ┌──────────────────────────────────────┐                    │
│  │ 전략: [알트 평균회귀 ▼]              │                    │
│  │ 기간: [2025-10-01] ~ [2026-03-28]    │                    │
│  │ 타임프레임: [4H ▼]                   │                    │
│  │ 초기 자본: [₩10,000,000]             │                    │
│  │                                      │                    │
│  │ [▶ 백테스트 실행]                     │                    │
│  └──────────────────────────────────────┘                    │
│                                                              │
│  ② 결과 영역 (실행 후 표시)                                   │
│  ┌──────────────────────────────────────────────────────────┐│
│  │ KPI 바: Sharpe 1.42 | 승률 58.3% | MDD -12.3% |          │
│  │         CAGR +45.2% | 거래 125건 | 평균보유 18h            │
│  │──────────────────────────────────────────────────────────││
│  │ 에쿼티 커브 (Recharts AreaChart)                          ││
│  │  ╱──╲    ╱──╲   ╱─────                                   ││
│  │ ╱    ╲──╱    ╲─╱       (BTC 가격 오버레이 옵션)           ││
│  │──────────────────────────────────────────────────────────││
│  │ 거래 내역 테이블                                          ││
│  │ 시각 | 종목 | 방향 | 진입가 | 청산가 | PnL | 사유         ││
│  │ ─────────────────────────────────────────────────────── ││
│  │ 03-25 | ETH | 매수 | ₩3.2M | ₩3.3M | +3.1% | z≥0 복귀  ││
│  │ 03-24 | SOL | 매수 | ₩152K | ₩148K | -2.6% | 손절       ││
│  └──────────────────────────────────────────────────────────┘│
└──────────────────────────────────────────────────────────────┘
```

**백테스트 설계 원칙:**
- 설정 → 실행 → 결과의 선형 흐름
- 실행 중: 프로그레스 바 (예상 소요시간 표시)
- 결과 KPI는 시그널 페이지와 동일 포맷
- 거래 내역은 정렬/필터 가능 (종목, 방향, PnL)
- "이 전략으로 가상매매 시작" CTA 버튼 (백테스트 → 가상매매 전환)

#### 인터랙션 상태

| 섹션 | 로딩 | 빈 상태 | 에러 |
|------|------|---------|------|
| 설정 패널 | — (즉시 표시) | — | 유효성 에러 인라인 표시 |
| 실행 | 프로그레스 바 + "실행 중... 약 10초" | — | "백테스트 실패" + 에러 메시지 + 재시도 |
| 결과 KPI | — | `BarChart3` + "백테스트를 실행하면 결과가 여기에 표시됩니다" | — |
| 에쿼티 커브 | `Loader2` 스피너 | (KPI와 동일 빈 상태) | — |
| 거래 내역 | 3행 스켈레톤 | "거래 내역 없음" | — |

#### 반응형

| 뷰포트 | 설정 패널 | 결과 영역 |
|---------|-----------|-----------|
| Desktop | 좌측 사이드 (280px) + 우측 결과 | KPI 6열 → 에쿼티 → 테이블 |
| Tablet | 상단 설정 (접을 수 있음) + 하단 결과 | KPI 3×2 |
| Mobile | 상단 설정 (접을 수 있음) | KPI 2×3, 테이블→카드 |

---

### 7.5 가상매매 페이지

> 핵심 목표: 실제 호가 기반 가상 매매. 다중 전략 동시 실행 + 성과 비교.

#### 정보 계층구조

```
┌──────────────────────────────────────────────────────────────┐
│  ① 세션 관리 바                                               │
│  [+ 새 세션] 활성 세션: 3/10  │ 전략 비교 모드 [토글]         │
│                                                              │
│  ② 세션 탭 / 전략 비교 뷰                                    │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │  [세션 A: 평균회귀] [세션 B: 도미넌스] [+ 추가]          │ │
│  │─────────────────────────────────────────────────────────│ │
│  │                                                         │ │
│  │  세션 A: 알트 평균회귀                                   │ │
│  │  시작: 2026-03-20 | 초기: ₩10M | 현재: ₩10.8M (+8.0%)  │ │
│  │                                                         │ │
│  │  가상 포지션:                                            │ │
│  │  종목│방향│진입가│현재가│미실현PnL│보유시간               │ │
│  │  ETH │매수│₩3.2M│₩3.3M│+3.1%   │12h                   │ │
│  │  SOL │매수│₩152K│₩148K│-2.6%   │8h                    │ │
│  │                                                         │ │
│  │  최근 체결:                                              │ │
│  │  [03-28 10:00] ETH 매수 z:-1.23 → 체결 ₩3,201,000     │ │
│  │  [03-27 22:00] DOGE 청산 z:+0.12 → PnL +1.8%          │ │
│  └─────────────────────────────────────────────────────────┘ │
│                                                              │
│  ③ 전략 비교 테이블 (비교 모드 ON 시)                         │
│  ┌─────────┬──────────┬──────┬──────┬───────┐               │
│  │전략     │수익률    │Sharpe│승률  │MDD    │               │
│  │평균회귀 │ +8.0%   │ 1.51 │61.2% │-4.3%  │               │
│  │도미넌스 │ +3.2%   │ 0.89 │52.1% │-6.1%  │               │
│  └─────────┴──────────┴──────┴──────┴───────┘               │
│                                                              │
│  ④ CTA: "이 전략으로 실전매매 전환 →" (검증 완료 시)          │
└──────────────────────────────────────────────────────────────┘
```

**가상매매 설계 원칙:**
- 탭으로 세션 전환 (최대 10개)
- 비교 모드: 모든 세션의 핵심 지표를 테이블로 나란히
- 가상 포지션은 실시간 호가 반영 (4H 캔들 시가 기준)
- "실전 전환" CTA는 세션이 최소 7일 이상 + 양수 수익일 때만 활성화

#### 인터랙션 상태

| 섹션 | 로딩 | 빈 상태 | 에러 |
|------|------|---------|------|
| 세션 탭 | — | `Play` + "첫 가상매매 세션을 시작하세요" + CTA "새 세션 만들기" | — |
| 가상 포지션 | shimmer 테이블 | `Inbox` + "아직 진입 조건을 충족한 종목이 없습니다" | "호가 데이터 실패" |
| 체결 내역 | shimmer 리스트 | "체결 기록이 쌓이면 여기에 표시됩니다" | — |
| 비교 테이블 | — | "2개 이상 세션이 필요합니다" | — |

#### 반응형

| 뷰포트 | 세션 탭 | 포지션 | 비교 |
|---------|---------|--------|------|
| Desktop | 가로 탭 | 테이블 | 테이블 |
| Tablet | 가로 탭 (스크롤) | 테이블 | 가로 스크롤 |
| Mobile | 드롭다운 선택 | 카드 리스트 | 카드 리스트 (세션별) |

---

### 7.6 포트폴리오 페이지

> 핵심 목표: 실제 거래소 자산 현황 + 거래 히스토리 조회.

#### 정보 계층구조

```
┌──────────────────────────────────────────────────────────────┐
│  ① 거래소 잔고 카드 (2열)                                     │
│  ┌──────────────────────┐ ┌──────────────────────┐           │
│  │ Upbit (현물)          │ │ OKX (선물)            │           │
│  │ ₩18,432,100          │ │ $12,340.56            │           │
│  │ ┌──────┬──────┬─────┐│ │ ┌──────┬──────┬─────┐│           │
│  │ │BTC   │1.2M  │+2.1%││ │ │ETH-P │$4.2K │+1.8%││           │
│  │ │ETH   │3.2M  │-0.4%││ │ │SOL-P │$2.1K │+3.2%││           │
│  │ │KRW   │14.0M │     ││ │ │USDT  │$6.0K │     ││           │
│  │ └──────┴──────┴─────┘│ │ └──────┴──────┴─────┘│           │
│  │ 연결 상태: ● 정상     │ │ 연결 상태: ○ 미설정  │           │
│  └──────────────────────┘ └──────────────────────┘           │
│                                                              │
│  ② 수익 차트 (Recharts, 일간/주간/월간 토글)                  │
│  [1D] [1W] [1M] [ALL]                                        │
│                                                              │
│  ③ 거래 히스토리 (필터 + 페이지네이션)                         │
│  필터: [전체 ▼] [전체 거래소 ▼] [최근 7일 ▼]                  │
│  시각 | 거래소 | 종목 | 방향 | 체결가 | 수량 | PnL            │
│  ──────────────────────────────────────────────               │
│  03-28 | Upbit | ETH | 매도 | ₩3.3M | 1.0 | +₩98K (+3.1%)   │
│  ...                                                         │
│  [이전] 1 2 3 ... [다음]                                      │
└──────────────────────────────────────────────────────────────┘
```

**포트폴리오 설계 원칙:**
- 거래소별 분리 표시 (업비트=KRW, OKX=USDT)
- API 미설정 거래소는 "API 키 설정 필요" + 설정 페이지 링크
- 거래 히스토리는 서버 사이드 페이지네이션 (20건/페이지)
- 수익 차트와 거래 내역은 같은 필터 공유

#### 인터랙션 상태

| 섹션 | 로딩 | 빈 상태 | 에러 |
|------|------|---------|------|
| 잔고 카드 | shimmer + `----` | `Key` + "API 키를 설정하면 잔고를 확인할 수 있습니다" + CTA "설정으로 이동" | "거래소 연결 실패" + 재시도 |
| 수익 차트 | `Loader2` 프레임 | (잔고와 동일 빈 상태) | "차트 데이터 실패" |
| 거래 히스토리 | 5행 스켈레톤 | `FileText` + "거래 기록이 없습니다" | "히스토리 로드 실패" |

#### 반응형

| 뷰포트 | 잔고 카드 | 히스토리 |
|---------|-----------|----------|
| Desktop | 2열 나란히 | 테이블 |
| Tablet | 2열 나란히 | 테이블 (가로 스크롤) |
| Mobile | 1열 스택 | 카드 리스트 |

---

### 7.7 설정 페이지

> 핵심 목표: API 키, 리스크 파라미터, 알림 설정.

#### 정보 계층구조

```
┌──────────────────────────────────────────────────────────────┐
│  ① API 키 관리                                                │
│  ┌──────────────────────────────────────────────────────────┐│
│  │ Upbit API                                     [● 연결됨] ││
│  │ Access Key: ••••••••abcd    [복사] [수정] [삭제]         ││
│  │ Secret Key: ••••••••efgh    마지막 확인: 2h 전           ││
│  │─────────────────────────────────────────────────────────││
│  │ OKX API                                       [○ 미설정] ││
│  │ [+ API 키 등록]                                          ││
│  │ 필요 권한: 읽기 + 거래 (출금 권한 금지)                   ││
│  └──────────────────────────────────────────────────────────┘│
│                                                              │
│  ② 리스크 파라미터                                            │
│  ┌──────────────────────────────────────────────────────────┐│
│  │ 일일 최대 손실    [2.0%  ] 기본: 2.0%                    ││
│  │ 단일 포지션 손실  [0.30% ] 기본: 0.30%                   ││
│  │ MDD 경고 한도     [15%   ] 기본: 15%                     ││
│  │ MDD 중단 한도     [25%   ] 기본: 25%                     ││
│  │ [저장] [기본값 복원]                                      ││
│  └──────────────────────────────────────────────────────────┘│
│                                                              │
│  ③ 알림 설정                                                  │
│  ┌──────────────────────────────────────────────────────────┐│
│  │ 텔레그램     [토글 OFF]  봇 토큰: [입력]                  ││
│  │ 디스코드     [토글 OFF]  웹훅 URL: [입력]                 ││
│  │ 알림 조건:                                                ││
│  │ ☑ 시그널 발생  ☑ MDD 경고  ☑ 레짐 전환  ☐ 체결 알림     ││
│  └──────────────────────────────────────────────────────────┘│
│                                                              │
│  ④ 데이터 관리 (위험 영역)                                    │
│  ┌──────────────────────────────────────────────────────────┐│
│  │ [캐시 초기화]  [가상매매 데이터 삭제]  [계정 삭제]        ││
│  │ ↑ 위험 작업은 빨간 보더 + 확인 다이얼로그                 ││
│  └──────────────────────────────────────────────────────────┘│
└──────────────────────────────────────────────────────────────┘
```

**설정 페이지 설계 원칙:**
- API 키는 마스킹 표시, 입력 시 `type="password"`
- 리스크 파라미터는 범위 제한 (예: MDD 5%~50%)
- 위험 작업(삭제)은 별도 섹션 + `--loss` 보더 + 확인 다이얼로그
- 저장 성공 시 토스트 "설정이 저장되었습니다" (`Check` 아이콘)

#### 인터랙션 상태

| 섹션 | 로딩 | 빈 상태 | 에러 |
|------|------|---------|------|
| API 키 | shimmer | "미설정" 배지 + 등록 버튼 | "키 검증 실패 — 권한을 확인하세요" |
| 리스크 파라미터 | shimmer | — (기본값 자동 적용) | "저장 실패" + 토스트 |
| 알림 | shimmer | "미설정" + 설정 가이드 링크 | "연결 테스트 실패" |
| 위험 작업 | 버튼 로딩 | — | 확인 다이얼로그에 에러 표시 |

#### 반응형

| 뷰포트 | 레이아웃 |
|---------|----------|
| Desktop | 1열, max-width 640px 중앙 정렬 |
| Tablet | 1열, 좌우 패딩 24px |
| Mobile | 1열, 좌우 패딩 16px |

---

### 7.8 온보딩 흐름

> 최초 로그인 사용자가 빈 대시보드를 보고 이탈하지 않도록.

```
Step 1: 환영 모달
┌────────────────────────────────────────┐
│  Coin Autopilot에 오신 것을 환영합니다  │
│                                        │
│  3단계로 시작할 수 있습니다:            │
│  ① 투자 성향 선택                      │
│  ② 첫 백테스트 실행                    │
│  ③ 가상매매 시작                       │
│                                        │
│  [시작하기 →]         [나중에]          │
└────────────────────────────────────────┘

Step 2: 투자 성향 선택 (전략 페이지로 이동)
→ 안전/중립/공격 중 선택

Step 3: 빠른 백테스트 (백테스트 페이지)
→ 기본 설정으로 자동 실행, 결과 확인

Step 4: 가상매매 시작 (가상매매 페이지)
→ "이 전략으로 가상매매 시작" 클릭

완료: 대시보드로 리다이렉트
→ 온보딩 완료 플래그 저장 (다시 안 뜸)
```

**온보딩 설계 원칙:**
- "나중에" 옵션 항상 제공 (강제 금지)
- 각 단계에서 이탈 가능 — 미완료 시 대시보드에 체크리스트 배너 표시
- 체크리스트: "✓ 성향 선택 완료 · ○ 백테스트 미실행 · ○ 가상매매 미시작"
- 온보딩 체크리스트 배너는 닫기(dismiss) 가능

---

### 7.9 사용자 여정 스토리보드

```
1. 발견 (비로그인)
   시그널 페이지 → "이 시그널, 근거가 다 보이네?" → 신뢰 형성
   ↓
2. 관심 (비로그인)
   백테스트 성과 확인 → "Sharpe 1.42면 괜찮은데?" → CTA "직접 해보기"
   ↓
3. 전환 (로그인)
   로그인 → 온보딩 → 투자 성향 선택 → 첫 백테스트
   ↓
4. 활성화 (가상매매)
   가상매매 시작 → 매일 포지션 확인 → "실제로 돈 벌 수 있겠는데"
   ↓
5. 유지 (실전)
   실전매매 전환 → 대시보드 모니터링 → 전략 비교/조정
   ↓
6. 확장
   새 전략 추가 → 다중 전략 운용 → 고급 파라미터 조정
```

**각 단계 감정 목표:**
- 발견: "투명하다, 신뢰할 수 있다"
- 관심: "성과가 괜찮다, 직접 확인하고 싶다"
- 전환: "쉽다, 막히는 곳이 없다"
- 활성화: "실제 시장과 동일하게 움직인다"
- 유지: "내 자산이 잘 관리되고 있다"
- 확장: "더 좋은 전략을 찾을 수 있다"

**이탈 방지 포인트:**
| 이탈 지점 | 원인 | 해결 |
|-----------|------|------|
| 시그널 페이지 → 이탈 | 근거가 안 보임 | 근거를 카드에 인라인 표시 |
| 로그인 후 → 이탈 | 빈 대시보드 | 온보딩 체크리스트 |
| 백테스트 후 → 이탈 | 다음 단계 불명확 | "가상매매 시작" CTA |
| 가상매매 중 → 이탈 | 성과 확인 번거로움 | 대시보드 KPI 자동 반영 |

---

## 8. DB 스키마

```sql
candles (exchange, symbol, timeframe, OHLCV, UNIQUE 복합키)
signals (strategy, symbol, direction, z_score, rsi, btc_regime, reasoning jsonb, is_active)
regime_states (regime, btc_close, ema_200, rsi_14, atr_pct)
backtest_results (user_id, strategy, params jsonb, sharpe, mdd, win_rate, equity_curve jsonb)
positions (user_id, session_type, strategy, symbol, pnl, status)

RLS:
  candles, signals, regime_states → anon SELECT 허용 (공개)
  backtest_results, positions → auth.uid() = user_id (개인)
  VPS 서버 → service_role key (RLS 우회)
```

---

## 9. 장애 대응

| 장애 | 대응 | 알림 |
|------|------|------|
| 업비트 WebSocket 끊김 | 지수 백오프 재연결 (1s→2s→4s→max 60s) | 3회 실패 시 알림 |
| 거래소 REST API 429 | 레이트 리밋 큐잉 (업비트 130ms/req) | 로그 경고 |
| 크론 실패 | 다음 크론에서 gap detection 보충 | 연속 2회 실패 시 알림 |
| Supabase 연결 실패 | 메모리 버퍼 임시 저장, 복구 시 일괄 쓰기 | 즉시 알림 |
| 비정상 시그널 (전 종목 동시 매수) | 5종목 초과 자동 억제 | 즉시 알림 |

---

## 10. 구현 현황

### Phase 0 — 프로젝트 셋업 ✅

- [x] C++ Agent 제거, 미사용 패키지 정리, 환경변수, CLAUDE.md 업데이트
- [x] Supabase 스키마 + RLS 정책 (`server/src/db/schema.sql`)

### Phase 1 — 핵심 전략 엔진 ✅

- [x] 전략 플러그인 인터페이스 (`strategy-base.ts`)
- [x] 지표 엔진 — EMA, RSI, ATR%, z-score, 볼린저밴드
- [x] 캔들 수집기 — backfill + 증분 + Upbit/OKX 레이트 리밋
- [x] BTC 레짐 필터 — Risk-On/Off 판단 + 경계값 처리
- [x] 알트 평균회귀 전략 — 진입/청산 로직, 파라미터 오버라이드
- [x] 간이 백테스트 엔진 — Decimal.js 수수료/슬리피지/Sharpe/MDD/CAGR
- [x] 6개월 히스토리 캔들 backfill (10개 심볼)

### Phase 2 — 서버 파이프라인 ✅

- [x] 시그널 생성 파이프라인 (`signal-generator.ts`)
- [x] node-cron 4시간 크론 + 6시간 Supabase 헬스체크
- [x] 시그널 REST API 3개 엔드포인트 (공개)

### Phase 3 — 공개 시그널 페이지 ✅

- [x] 용어 설명 툴팁 컴포넌트 (TermTooltip, 12개 용어)
- [x] 공개 시그널 페이지 (레짐 히어로, 시그널 테이블/카드, 진입 근거 확장 패널)
- [x] Supabase 클라이언트 + React Query 연결
- [x] 스켈레톤 로딩, Risk-Off 빈 상태, 에러 상태
- [x] Terminal Ink 디자인 시스템 전면 적용 (DESIGN.md 준수)

### Phase 4 — 인증 + 개인 기능 🔵 (진행 중)

- [x] Supabase Auth — 이메일 매직 링크 + GitHub OAuth (`useAuth.ts`)
- [x] 로그인 모달 — GitHub OAuth 버튼, 이메일 폼, 성공/에러 상태 (`LoginModal.tsx`)
- [x] Header 인증 통합 — 비로그인 "로그인" 버튼, 로그인 시 아바타 + 로그아웃
- [x] AuthGuard — 비공개 라우트 인라인 로그인 안내 + CTA (`AuthGuard.tsx`)
- [x] 서버 백테스트 라우트 — Zod 검증 → 캔들 로드 → 엔진 실행 → DB 저장 (`POST /run`, `GET /results`, `GET /results/:id`)
- [x] 서버 대시보드 라우트 — Supabase 실데이터 (레짐, 시그널, 백테스트 성과, 에퀴티 히스토리)
- [x] 웹 백테스트 페이지 — 파라미터 입력, useMutation 실행, KPI 6개, Recharts 에퀴티 커브, 거래 내역 정렬 테이블
- [x] 웹 대시보드 페이지 — Supabase 직접 쿼리 (Sharpe, 승률, MDD, 활성 시그널), 시그널 피드
- [ ] 서버 전략 라우트 — CRUD 5개 엔드포인트 (현재 스텁)
- [ ] 웹 전략 페이지 — 서버 연동 + 파라미터 저장 (현재 하드코딩 UI 셸)
- [ ] 온보딩 흐름 — 환영 모달 + 3단계 체크리스트

### Phase 5 — 가상매매 ⬜

- [ ] 가상매매 엔진 (서버) — 호가 기반 가상 체결, 포지션 관리
- [ ] 서버 가상매매 라우트 — 세션 CRUD, 가상 포지션 (현재 스텁)
- [ ] 웹 가상매매 페이지 — 세션 탭, 포지션 테이블, 비교 모드 (현재 빈 셸)
- [ ] 다중 전략 비교 대시보드

### Phase 6 — 포트폴리오 + 설정 ⬜

- [ ] 서버 포트폴리오 라우트 — 거래소 잔고 조회, 거래 내역 (현재 스텁)
- [ ] 서버 설정 라우트 — API 키 관리, 리스크 파라미터 CRUD (현재 스텁)
- [ ] 웹 포트폴리오 페이지 — 거래소 잔고 카드, 수익 차트, 거래 히스토리 (현재 UI 셸)
- [ ] 웹 설정 페이지 — API 키, 리스크 파라미터, 알림 설정 (현재 UI 셸)

### 추가 과제 ⬜

- [ ] 전략 2~5 구현 (도미넌스 로테이션, 변동성 타이밍, 펀딩비 차익, 김프 모니터)
- [ ] 본격 백테스트 (생존편향 제거, 워크포워드 70/30)
- [ ] 텔레그램/디스코드 알림
- [ ] OG 태그 (공유 미리보기)
- [ ] Header 실시간 BTC 가격/레짐 (현재 "--" 하드코딩)
- [ ] StatusBar 실데이터 연결 (현재 하드코딩)
- [ ] Zustand 스토어 정리 또는 제거 (5개 모두 미사용, React Query 직접 사용 중)
- [ ] WebSocket 실시간 통신 (server/src/websocket/ 비어있음)

---

## 11. 리뷰 이력

| 리뷰 | 일시 | 결과 |
|------|------|------|
| `/office-hours` | 2026-03-27 | APPROVED — 디자인 문서 생성 |
| `/plan-eng-review` | 2026-03-27 | CLEARED — 7 이슈, 32 테스트 GAP, 4 critical gap |
| `/plan-design-review` | 2026-03-27 | CLEARED — 4/10 → 8/10, 5 디자인 결정 |
| `/design-outside-voices` | 2026-03-28 | Codex HARD REJECT + Claude 14건 발견. 섹션 7 전체 재작성, 6개 페이지 UI 명세 추가, 온보딩/사용자 여정/글로벌 규칙 추가 |
